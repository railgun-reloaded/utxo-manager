{
  "name": "@railgun-reloaded/utxo-solver",
  "version": "0.0.1",
  "description": "UTXO algorithmic solver for selecting spending-solutions.",
  "homepage": "https://github.com/railgun-reloaded/utxo-solver#readme",
  "bugs": {
    "url": "https://github.com/railgun-reloaded/utxo-solver/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/railgun-reloaded/utxo-solver.git"
  },
  "license": "MIT",
  "author": "RAILGUN Project Contributors",
  "engines": {
    "node": ">=22"
  },
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "test": "npm run build && node --test ./test/**/*.test.js",
    "test-only": "npm run build && node --test-only ./test/*.test.js",
    "test:coverage": "npm run build && c8 --reporter=text --reporter=html node --test ./test/**/*.test.js"
  },
  "devDependencies": {
    "@railgun-reloaded/eslint-config": "^1.1.0",
    "@railgun-reloaded/tsconfig": "^1.0.1",
    "@types/node": "^22.13.10",
    "c8": "^10.1.3",
    "eslint": "^9.22.0",
    "typescript": "^5.8.2"
  }
}
